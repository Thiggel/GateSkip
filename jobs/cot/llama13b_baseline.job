#!/bin/bash -l

#SBATCH --partition=gpu_h100
#SBATCH --gpus=1
#SBATCH --cpus-per-task=16
#SBATCH --ntasks-per-node=1
#SBATCH --time=24:00:00
#SBATCH --job-name=llama13b_baseline
#SBATCH --output=job_logs/cot/llama13b_baseline-%A.out

cd "$HOME/GateSkip"

. jobs/environment.sh

# srun "${APPTAINER_RUN[@]}" "${PYTHON_BIN}" -m experiment \
#  --model-name "TroyDoesAI/Llama-3.1-13B-Instruct" \
#  --dataset csqa_gsm8k_gen \
#  --pretrained \
#  --batch-size 1 \
#  --eval-batch-size 1 \
#  --seq-length 4096 \
#  --learning-rate 3e-6 \
#  --warmup-steps 1000 \
#  --lr-decay-steps 6800 \
#  --max-epochs 1 \
#  --max-hours 2 \
#  --finetune-mode full \
#  --experiment-name llama13b_baseline \
#  --save-to-checkpoint llama13b_baseline \
#  --project-name gateskip \
#  --use-deepspeed \
#  --use-gradient-checkpointing \
#  --enable-logging
 
srun "${APPTAINER_RUN[@]}" "${PYTHON_BIN}" -m experiment \
  --model-name "TroyDoesAI/Llama-3.1-13B-Instruct" \
  --pretrained \
  --use-gating \
  --no-one-gate-per-layer \
  --skip-modules \
  --randomly-skip \
  --no-actually-gate \
  --eval-batch-size 1 \
  --seq-length 4096 \
  --finetune-mode full \
  --mode evaluate \
  --evaluation-metrics csqa_gen,gsm8k \
  --num-fewshot 0 \
  --experiment-name llama13b_baseline \
  --load-from-checkpoint llama13b_baseline \
  --project-name gateskip
